public class test {

   private double[] purchase; 

   public static void main(String[] args) {
      
      for (int i = 0; i < purchaseIn.length; i++) {
      
         purchase = Arrays.copyOf(purchase, purchase.length + 1);
         purchase[purchase.length - 1] = purchaseIn[i];
      }
   
   }
}